version: 0.2

phases:
  install:
    runtime-versions:
      php: 8.1
    commands:
      - echo "Installing dependencies (none needed for simple PHP site)"
  build:
    commands:
      - echo "Build phase skipped. No compilation required."
  post_build:
    commands:
      - echo "Zipping the application for deployment"
      - zip -r php-app.zip .  # include everything in the artifact

artifacts:
  files:
    - php-app.zip
